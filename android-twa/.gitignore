# Generated by `bubblewrap init` / `bubblewrap build` from twa-manifest.json.
# Only the config and docs are tracked; the Android project and secrets are not.
app/
build/
.gradle/
gradle/
gradlew
gradlew.bat
build.gradle
settings.gradle
*.keystore
*.apk
*.aab
*.idsig
store_icon.png
